package de.einnik.yamler_v3.core.enums

/**
 * The mode the keys in the YAML file are named
 *
 * @author EinNik
 * @since 3.0.0-SNAPSHOT
*/
enum class ConfigMode {
    DEFAULT,
    PATH_BY_UNDERSCORE,
    FIELD_IS_KEY
}